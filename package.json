{
  "name": "application",
  "license": "MIT",
  "engines": {
    "node": "12.11.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "deploy": "cp -a /build/. public/",
    "heroku-postbuild": "cd .. & npm run build && rmdir client/node_modules && npm run deploy"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
